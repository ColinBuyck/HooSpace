import { View } from "react-native";

//Occupancy percentage graphic to be used on ccordion headers
export const ProgressGraphic = (occupancy: number, capacity: number) => {
    return (
        <View>
        </View>
    );
}